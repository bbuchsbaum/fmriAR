% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acvf_bias.R
\name{acvf_bias_matrix}
\alias{acvf_bias_matrix}
\title{Bias matrix relating raw residual autocovariance to the truth}
\usage{
acvf_bias_matrix(design, runs = NULL, censor = NULL, max_lag = 20L)
}
\arguments{
\item{design}{Numeric design matrix (timepoints x regressors), the one whose
projection produced the residuals.}

\item{runs}{Optional run labels, length \code{nrow(design)}. Lag products never
cross a run boundary.}

\item{censor}{Optional indices of censored timepoints (1-based), excluded
exactly as in \code{\link[=fit_noise]{fit_noise()}}.}

\item{max_lag}{Highest lag to correct.}
}
\value{
A named list of \code{(max_lag + 1)} square matrices, one per run, rows
indexed by the lag of the raw estimate and columns by the lag of the truth.
}
\description{
Builds the linear map \code{A} satisfying \verb{E[gamma_raw] = A gamma_true} induced by
projecting a design out of the data. Autocovariance estimated from GLM
residuals is biased low, increasingly so for richer designs, because
\verb{E[ehat ehat'] = M Sigma M} for the residual-forming projection \code{M}. Solving
\verb{A gamma = gamma_raw} removes that bias.
}
\details{
Ordinarily you do not need this: pass \code{design} to \code{\link[=fit_noise]{fit_noise()}} and it builds
and applies the matrices itself. Use this directly to inspect the bias, or to
cache the matrices when many datasets share one design and segmentation and
feed them back as \code{acvf_correction}.

One matrix is returned per run, because \code{\link[=fit_noise]{fit_noise()}} estimates each run
separately and the residual operator restricted to a run still involves the
whole design.

The correction is exact for noise whose autocovariance dies within \code{max_lag}.
Under long memory it is partial, since truncating the system aliases in
structure past the budget. With high-pass filtering (DCT, 128 s) a budget
near 20-25 lags is typically enough; without high-pass it exceeds 150 and the
approach stops being practical.

Cost is \code{O(max_lag^2 * n_valid * n)} per run, so it is worth caching.
}
\examples{
n <- 120
X <- cbind(1, poly(seq_len(n), 3))
A <- acvf_bias_matrix(X, max_lag = 5)
round(A[[1]][1:3, 1:3], 3)
}
\seealso{
\code{\link[=fit_noise]{fit_noise()}}, \code{\link[=noise_acvf]{noise_acvf()}}
}
