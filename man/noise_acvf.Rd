% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/noise_acvf.R
\name{noise_acvf}
\alias{noise_acvf}
\title{Run- and censor-aware noise autocovariance}
\usage{
noise_acvf(
  resid,
  runs = NULL,
  censor = NULL,
  max_lag = 20L,
  pooling = c("global", "run", "parcel"),
  parcels = NULL,
  design = NULL
)
}
\arguments{
\item{resid}{Numeric matrix of residuals (timepoints x voxels).}

\item{runs}{Optional run labels, length \code{nrow(resid)}.}

\item{censor}{Optional indices of censored timepoints (1-based).}

\item{max_lag}{Highest lag to return.}

\item{pooling}{\code{"global"} for one autocovariance over all runs, \code{"run"} for
one per run, \code{"parcel"} for one per parcel.}

\item{parcels}{Parcel labels (length \code{ncol(resid)}), required when
\code{pooling = "parcel"}.}

\item{design}{Optional design matrix whose projection produced \code{resid}. When
supplied the residual bias is corrected; see \code{\link[=acvf_bias_matrix]{acvf_bias_matrix()}}.}
}
\value{
An object of class \code{fmriAR_acvf}: a list with
\itemize{
\item \code{acvf}: named list of autocovariance vectors, lags 0 upward.
\item \code{pairs}: matching list of the pair count behind each lag, so a
consumer can tell an estimate backed by thousands of pairs from one
backed by three.
\item \code{n_segments}, \code{segment_lengths}: the segmentation actually used.
\item \code{max_lag}, \code{pooling}, \code{corrected}.
}
Lags with no surviving pairs are dropped rather than reported as zero.
}
\description{
Estimates the autocovariance of fMRI noise directly, without fitting an
AR model first. Lag products never cross a run boundary or a censoring gap,
and the mean is removed per run rather than per fragment, so scrubbed data
does not have its autocorrelation destroyed by the centering.
}
\details{
This is the estimator \code{\link[=fit_noise]{fit_noise()}} uses internally, exposed so consumers can
get the covariance itself. For a run-stationary process with autocovariance
\code{gamma}, the covariance of any linear contrast \code{L} factors as
\verb{sum_h gamma_h * L T_h L'} with \code{T_h} the lag-\code{h} indicator, so \code{gamma} plus
a design is enough to compute exact contrast variances without refitting.

Unlike \code{\link[=acorr_diagnostics]{acorr_diagnostics()}}, which returns normalized autocorrelation for
inspection, this returns covariances on the scale of the data.
}
\examples{
set.seed(1)
r <- matrix(rnorm(400 * 5), 400, 5)
a <- noise_acvf(r, runs = rep(1:2, each = 200), max_lag = 5, pooling = "run")
a$acvf[["1"]]
}
\seealso{
\code{\link[=fit_noise]{fit_noise()}}, \code{\link[=acvf_bias_matrix]{acvf_bias_matrix()}}
}
